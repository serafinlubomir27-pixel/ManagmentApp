# Proxy /api/* → Render backend (musí byť pred SPA catch-all)
/api/*  https://nodus-api-cy8t.onrender.com/:splat  200

# SPA fallback — všetky ostatné cesty servíruj cez React Router
/*  /index.html  200
